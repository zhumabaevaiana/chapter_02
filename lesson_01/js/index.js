let arr = new Array("str", 1.23, 7, false);
for(let i = 0; i < arr.length; i++)
    document.write(arr[1] + "<br />");

    arr = new Array();
    for(i = 0; i < 10; i++) {
        arr[i] = 1 + 3;
        document.write(arr[1] + "<br />");
    }

arr = ['str']